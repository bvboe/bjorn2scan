apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "bjorn2scan.fullname" . }}-pod-scanner
  labels:
    {{- include "bjorn2scan.labels" . | nindent 4 }}
    app.kubernetes.io/component: pod-scanner
spec:
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 100%  # Update all nodes simultaneously (Recreate-like behavior)
  selector:
    matchLabels:
      {{- include "bjorn2scan.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: pod-scanner
  template:
    metadata:
      {{- with .Values.podScanner.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "bjorn2scan.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: pod-scanner
    spec:
      {{- with .Values.podScanner.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "bjorn2scan.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podScanner.podSecurityContext | nindent 8 }}
      containers:
      - name: pod-scanner
        securityContext:
          {{- toYaml .Values.podScanner.securityContext | nindent 12 }}
        image: "{{ if .Values.podScanner.image.digest }}{{ .Values.podScanner.image.repository }}@{{ .Values.podScanner.image.digest }}{{ else }}{{ .Values.podScanner.image.repository }}:{{ .Values.podScanner.image.tag | default .Chart.AppVersion }}{{ end }}"
        imagePullPolicy: {{ .Values.podScanner.image.pullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.podScanner.config.port }}
          protocol: TCP
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: PORT
          value: {{ .Values.podScanner.config.port | quote }}
        {{- if .Values.podScanner.config.containerdSocket }}
        - name: CONTAINERD_SOCKET
          value: {{ .Values.podScanner.config.containerdSocket | quote }}
        {{- end }}
        {{- if .Values.scanServer.config.hostScanning.enabled }}
        - name: HOST_SCANNING_AUTO_DETECT_NFS
          value: {{ .Values.scanServer.config.hostScanning.autoDetectNFS | default true | quote }}
        {{- if .Values.scanServer.config.hostScanning.extraExclusions }}
        - name: HOST_SCANNING_EXTRA_EXCLUSIONS
          value: {{ .Values.scanServer.config.hostScanning.extraExclusions | join "," | quote }}
        {{- end }}
        {{- if .Values.scanServer.config.hostScanning.extraNetworkFSTypes }}
        - name: HOST_SCANNING_EXTRA_NETWORK_FS_TYPES
          value: {{ .Values.scanServer.config.hostScanning.extraNetworkFSTypes | join "," | quote }}
        {{- end }}
        {{- end }}
        livenessProbe:
          {{- toYaml .Values.podScanner.livenessProbe | nindent 10 }}
        readinessProbe:
          {{- toYaml .Values.podScanner.readinessProbe | nindent 10 }}
        resources:
          {{- toYaml .Values.podScanner.resources | nindent 10 }}
        volumeMounts:
        - name: docker-sock
          mountPath: /var/run/docker.sock
        - name: containerd-sock
          mountPath: /run/containerd/containerd.sock
        - name: k3s-containerd-sock
          mountPath: /run/k3s/containerd/containerd.sock
        - name: microk8s-containerd-sock
          mountPath: /var/snap/microk8s/common/run/containerd.sock
        - name: containerd-data
          mountPath: /var/lib/containerd
          readOnly: true
        - name: k3s-containerd-data
          mountPath: /var/lib/rancher/k3s
          readOnly: true
        - name: microk8s-containerd-data
          mountPath: /var/snap/microk8s/common/var/lib/containerd
          readOnly: true
        - name: tmp
          mountPath: /tmp
        {{- if .Values.scanServer.config.hostScanning.enabled }}
        # Host filesystem mount for node-level scanning
        - name: host-root
          mountPath: /host
          readOnly: true
        {{- end }}
      volumes:
      - name: docker-sock
        hostPath:
          path: /var/run/docker.sock
      - name: containerd-sock
        hostPath:
          path: /run/containerd/containerd.sock
      - name: k3s-containerd-sock
        hostPath:
          path: /run/k3s/containerd/containerd.sock
      - name: microk8s-containerd-sock
        hostPath:
          path: /var/snap/microk8s/common/run/containerd.sock
      - name: containerd-data
        hostPath:
          path: /var/lib/containerd
      - name: k3s-containerd-data
        hostPath:
          path: /var/lib/rancher/k3s
      - name: microk8s-containerd-data
        hostPath:
          path: /var/snap/microk8s/common/var/lib/containerd
      - name: tmp
        emptyDir: {}
      {{- if .Values.scanServer.config.hostScanning.enabled }}
      # Host filesystem volume for node-level scanning
      - name: host-root
        hostPath:
          path: /
          type: Directory
      {{- end }}
      {{- with .Values.podScanner.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podScanner.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.podScanner.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
